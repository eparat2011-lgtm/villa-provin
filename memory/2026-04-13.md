
## Nacht-Aufgaben (bis morgen früh erledigen)

### Kritische Bugs die heute €1.000+ gekostet haben:

1. **Manueller Trade Handler** — komplett neu schreiben
   - Long/Short via Webhook öffnen muss: Order + active_trade + journal + signal_log + SL korrekt setzen
   - is_reentry Flag korrekt setzen wenn Re-Entry
   - Nie wieder manuell nur Order platzieren ohne Registrierung

2. **active_trade Synchronisation**
   - Beim Server-Neustart: active_trade.json MIT Bybit synchronisieren
   - Wenn Position auf Bybit offen aber nicht in active_trade → automatisch registrieren
   - Richtung (long/short) korrekt schreiben

3. **Race-Condition bei simultanen Signalen**
   - Long + Sell Signal gleichzeitig → nur erstes verarbeiten, zweites ignorieren (1 Sek Sperre)

4. **SL-Logik bei manuellen Trades**
   - is_reentry immer explizit setzen
   - TP1 Long normal: SL → -2% (NICHT Breakeven)
   - TP1 Long Re-Entry: SL → -2% + 25% schließen

5. **End-to-End Tests vor Live-Deployment**
   - Jede neue Funktion mit Test-Signal testen
   - Qty-Berechnung für alle 4 Symbole testen

### Neue Regel:
Kein SL nachziehen außer bei echtem TP-Signal von Infinity Algo.
